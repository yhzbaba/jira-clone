bunx --bun shadcn@latest init

bunx --bun shadcn@latest add button

npx shadcn@latest add avatar

npx shadcn@latest add avatar

bun add hono

bun add @tanstack/react-query@5.59.0

bun add @hono/zod-validator

bun add node-appwrite@14.1.0

sudo bun add server-only

前端到中间件的调用逻辑：
1、(@/app/(auth)/sign-up.page.tsx) tsx中：
const {mutate} = useRegister(); 来自@/features/auth/api/use-register



1:
VLLM_LOGGING_LEVEL=WARNING CUDA_VISIBLE_DEVICES=1,2,3 vllm serve Qwen/Qwen2-7B-Instruct --max-model-len=4096 --max-num-seqs=1 --disable-log-requests --gpu-memory-utilization=0.80 --trust-remote-code

/home/suzyhzbaba/miniconda3/envs/cosyvoice/bin/python download.py

2:
PYTHONPATH=third_party/Matcha-TTS /home/suzyhzbaba/miniconda3/envs/cosyvoice/bin/python api.py

webui
git clone https://github.com/open-webui/open-webui.git
cd open-webui/

# Copying required .env file
cp -RPp .env.example .env

# Building Frontend Using Node
npm install
npm run build

cd ./backend

# Optional: To install using Conda as your development environment, follow these instructions:
# Create and activate a Conda environment
conda create --name open-webui-env python=3.11
conda activate open-webui-env

# Install dependencies
pip install -r requirements.txt -U

# Start the application
3:
bash start.sh
